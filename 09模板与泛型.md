## 模板与泛型

函数模板是C++中泛型编程的核心，用于定义通用的函数，允许参数或返回值类型在调用时指定。以下是基本语法和示例：

```cpp
template <typename T> 		// 或 template <class T>
返回类型 函数名(参数列表) {
    // 函数体
}
```

-   T 是模板参数，调用时替换为具体类型。
-   可有多个模板参数：`template <typename T1, typename T2>`。
-   可结合非模板参数使用。

示例

1.  简单函数模板

```cpp
template <typename T>
T max(T a, T b) {
    return (a > b) ? a : b;
}

int main() {
    int x = 5, y = 3;
    double a = 2.5, b = 4.7;
    std::cout << max(x, y) << std::endl; // 输出 5
    std::cout << max(a, b) << std::endl; // 输出 4.7
    return 0;
}
```

-   max 自动适配 int 和 double 类型。

1.  多模板参数

```cpp
template <typename T1, typename T2>
void print(T1 a, T2 b) {
    std::cout << a << ", " << b << std::endl;
}

int main() {
    print(42, "hello"); // 输出 42, hello
    print(3.14, true);  // 输出 3.14, 1
    return 0;
}
```

1.  显式指定类型

```cpp
template <typename T>
T add(T a, T b) {
    return a + b;
}

int main() {
    std::cout << add<double>(2, 3.5) << std::endl; // 显式指定 T 为 double，输出 5.5
    return 0;
}
```

注意事项

-   类型推导：编译器通常自动推导类型，但复杂情况需显式指定。
-   特化：可为特定类型定义特化版本。

```cpp
template <>
const char* max<const char*>(const char* a, const char* b) {
    return (std::strcmp(a, b) > 0) ? a : b;
}
```

-   头文件：模板定义通常放在头文件中，因编译时需完整定义。
-   限制：模板参数需支持函数体内操作（如 >、 +），否则编译失败。

高级用法

-   非类型模板参数：

```cpp
template <int N>
int multiply(int x) {
    return x * N;
}
```

-   可变参数模板：

```cpp
template <typename... Args>
void printAll(Args... args) {
    (std::cout << ... << args) << std::endl;
}
```