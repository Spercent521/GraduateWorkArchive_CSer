# 异常处理

## 关键字

### try

定义可能抛出异常的代码块。try块中的代码会被监控，如果有异常抛出，控制权会转移到对应的catch块。

### throw

用于抛出异常。可以用任何类型（如int、double、std::string、自定义类型等）作为异常对象。

### catch

定义捕获并处理特定类型异常的代码块。可以有多个catch块，按顺序匹配异常类型。

## 异常处理流程

1.  程序在try块中执行代码。

2.  如果throw语句被执行，程序立即退出try块，寻找匹配的catch块。

3.  **异常类型与catch块的类型匹配（从上到下，第一个匹配的catch块被执行）**

4.  如果没有匹配的catch块，程序调用std::terminate()终止运行（通常导致程序崩溃）。

5.  catch块处理完异常后，程序继续执行try-catch结构之后的代码。

    ## 异常处理特点

-   遵循严格的类型匹配。如果我`throw -1`，即使在`catch int`之前`catch double`，也会被前者捕获，而不会被后者捕获。

-   可以用`catch(...)`捕获所有类型的异常（通配符捕获），但应谨慎使用，因为它可能掩盖未预期的错误。
-   性能开销：
    -   异常处理有一定的性能开销，因此不建议在性能敏感的代码中频繁使用异常来控制正常流程。
    -   异常更适合处理“异常情况”（如除零、文件打开失败等）。
-   异常安全性：
    -   抛出异常时，C++会自动调用栈上对象的析构函数（栈解退，stack unwinding），确保资源被正确清理。
    -   建议使用RAII（资源获取即初始化）技术管理资源（如std::unique_ptr、std::fstream等），以保证异常安全。
## 异常的再抛出

如果一个函数在执行的过程中，抛出的异常在本函数内就被catch块捕获并处理了，那么该异常就不会抛给这个函数的调用者(也称 “上一层的函数”)；

**如果异常在本函数中没被处理，就会被抛给上一层的函数。**

## C++异常标准类

在 C++ 中，标准库提供了专门用于异常处理的标准异常类，这些类定义在 `<stdexcept> `头文件中，并以 `std::exception` 为基类。使用标准异常类相比抛出原始类型（如 int 或 double）更规范、更安全，因为它们提供了统一的接口（如错误信息）和类型层次结构，便于捕获和处理异常。以下是对 C++ 标准异常类的详细讲解，包括类层次结构、常用类及其用途。

```plain
std::exception								(基类)
├── std::logic_error 						 (逻辑错误,表示程序逻辑错误，通常由程序员的编码错误引起)
│   ├── std::invalid_argument				  	(参数不符合函数要求,如传递非法参数)
│   ├── std::domain_error					  	(数学函数的输入超出定义域,如对负数求平方根）)
│   ├── std::length_error						(尝试超过容器最大长度,如 std::vector 超出 max_size() )
│   ├── std::out_of_range						(访问容器或字符串的非法索引,如 std::vector::at()越界)
│   └── std::future_error						 (C++11,与 std::future 或 std::promise 相关的异步操作错误)
└── std::runtime_error 						(运行时错误，通常与程序运行环境相关，如文件不可用、内存不足)
    ├── std::range_error						(计算结果超出有效范围,如数值溢出)
    ├── std::overflow_error						(算术运算溢出,如整数除法溢出)
    ├── std::underflow_error					(算术运算下溢,如浮点数精度丢失)
    ├── std::system_error						 (C++11，系统调用或操作系统相关的错误（如文件打开失败）)
    └── std::ios_base::failure 					(I/O 操作失败，如文件读写错误)			
```

-   `std::bad_cast`
    -   头文件:`<typeinfo>`
    -   当使用 dynamic_cast 进行类型转换失败时抛出此异常。
    -   仅在将多态类型的指针或引用转换为不匹配的类型时触发。
    -   用于安全地执行运行时类型转换（通常在多态类层次结构中）。
-   `std::bad_alloc`
    -   头文件：`<new>`
    -   当 new 操作符无法分配内存时抛出此异常（通常是内存不足）。
    -   处理动态内存分配失败的场景。

## 未处理异常

如果异常在`try`块以外抛出，或者抛出的异常没有被任何`catch`块捕获，程序会调用`std::terminate()`。

`std::terminate() `默认会调用 `abort()`，终止程序运行，通常会导致程序崩溃（**不会执行栈解退或清理**）。







