## 虚函数与多态

### final和override关键字

C++11中：

1.   可以用关键字override来显式说明派生类的函数要覆盖基类的虚函数。

2.   可以使用关键字final阻止派生类覆盖基类版本的虚函数。

```c++
struct B {
	virtual void fun1(int) { }
	virtual void fun2() { }
	void fun3() { }
};

struct D1 : public B {
	void fun1() override { } 		//错误：基类没有不带参数的fun1函数
	void fun2() final { } 			//D1::fun2为最终版本
	void fun3() override { } 		//错误：基类没有可覆盖的函数
};

struct D2 : public D1 {
	void fun2() { } 			    //错误：不允许覆盖基类D1中的fun2函数
};

```

### 8.1 多态性概述

多态：调用同一个函数名的函数，实现完全不同的功能。一种形式多种实现。

#### C++多态的两种类型

*   编译时多态：在编译时确定使用哪一个函数。如，函数重载、运算符重载、模板实例化。也称**静态多态**。
*   运行时多态：在运行时确定使用哪一个函数。如，通过类继承关系和虚函数实现（运行时才创建对象，从而确定被调用函数）。也称**动态多态**。

#### 运行时多态的基础

*   **基类指针**：
    *   派生类对象可以安全地赋值给基类指针（向上转型）。
    *   通过基类指针调用基类和派生类中的同名虚函数时：
        *   如果该指针指向的是一个基类对象，那么被调用的是基类的虚函数；
        *   如果该指针指向的是一个派生类对象，那么被调用的是派生类的虚函数；
*   **基类引用**：
    *   派生类对象可以绑定到基类引用
    *   通过基类引用调用基类和派生类中的同名虚函数时
        *   如果该引用指向的是一个基类对象，那么被调用的是基类的虚函数；
        *   如果该引用指向的是一个派生类对象，那么被调用的是派生类的虚函数；

**总的来说，对于运行时多态，可以用基类指针来调用相应的函数。**

### 8.2 虚函数与多态

#### 基础

1.   关键字`virtual`指明该成员函数为虚函数。`virtual`仅用于类定义中，如虚函数在类外定义，不可再加`virtual`。

2.   当一个类的某个成员函数被定义为虚函数，则由该类的所有派生类中该函数始终保持虚函数的特征。

3.   静态成员函数不能是虚函数。

     1.   **静态成员函数不依赖于对象实例**

     -   静态成员函数的特性：
         -   **静态成员函数属于类本身，而不属于任何具体的对象实例。**
         -   调用静态成员函数时，不需要通过对象实例，可以直接通过类名调用（如`ClassName::staticFunction()`）。
         -   静态成员函数内部无法访问非静态成员（包括this指针），因为它没有与任何对象绑定。
     -   虚函数的要求：
         -   虚函数的动态分派依赖于对象的`vptr`，而`vptr`存储在对象实例中。
         -   **动态绑定需要知道调用者的动态类型（即对象实际类型），这只有在存在具体对象时才有可能。**
     -   冲突：
         -   由于静态成员函数不与任何对象实例关联，它无法访问`vptr`，也无法通过`vtable`进行动态分派。
         -   如果静态成员函数被声明为虚函数，运行时无法确定调用哪个类的函数版本（基类还是派生类），因为没有对象上下文来提供动态类型信息。

     2.   **语义上的不合理性**

          *   虚函数的设计目的是实现基于对象类型的多态行为，而静态成员函数的调用与对象无关，更多用于类级别的操作（如工具函数、全局状态管理）。

          *   允许静态成员函数成为虚函数会违背C++的多态设计初衷，增加语义混淆。例如，调用`Base::staticFunction()`时，期望的是类级别的行为，而不是基于某个对象的动态行为。

     3.   **语言标准规定**

          *   编译不会通过。

4.   内联函数不能是虚函数。但如果编译器能够通过**去虚化devirtualization**优化确定虚函数的调用目标（例如在某些静态分析场景中明确知道对象的类型），它可能将虚函数调用替换为直接调用并内联。但这依赖于编译器的优化能力，不是语言的默认行为。

     1.   **内联函数的特性**

     -   内联函数的定义：

         -   **内联函数通过inline关键字建议编译器将函数调用替换为函数体的直接代码（内联展开），以减少函数调用的开销。**
         -   内联是编译期优化，由编译器决定是否实际内联，具体取决于函数复杂度、调用频率等因素。

     -   注意：

         -   **inline只是一个建议，编译器可能忽略它（例如函数体过大或包含复杂控制流时）。**
         -   内联函数仍然是普通的函数，具有自己的地址，可以被调用。

     2.   虚函数调用是通过`vtable`在运行时解析的，调用地址在运行时通过`vptr`确定。这种运行时解析与内联的编译期展开存在矛盾：内联要求在编译时将函数调用替换为具体代码，但虚函数的实际调用地址在编译时未知（因为它依赖于对象的动态类型）。

     3.   **具体情况**

          *   通过对象直接调用：如果通过对象直接调用虚函数（例如`obj.virtualFunction()`，obj是具体类型的对象），编译器知道对象的静态类型，可以优化为直接调用，并可能内联函数体。

          *   通过基类指针或引用调用：如果通过基类指针或引用调用虚函数（例如`basePtr->virtualFunction()`），编译器无法在编译时确定实际调用的函数地址，因此无法内联，只能通过`vtable`进行运行时分派。

5.   包含虚函数的类称为**多态类**。

>   [!NOTE]
>
>   派生类定义与基类同名虚函数时，参数表和返回类型必须全部与基类中的虚函数一致。
>
>   析构函数可定义为虚函数，构造函数不能定义成虚函数。因为在调用构造函数时对象还没有完成实例化。在基类中及其派生类中都动态分配的内存空间时，必须把析构函数定义为虚函数，实现撤消对象时的多态性。

```c++
#include <iostream>
using namespace std;

// 基类 Animal
class Animal {
public:
    // 虚函数，用于多态
    virtual void makeSound() {
        cout << "动物发出声音" << endl;
    }

    // 虚析构函数，确保派生类析构时正确调用基类析构函数
    virtual ~Animal() {
        cout << "Animal destructor called" << endl;
    }
};

// 派生类 Dog
class Dog : public Animal {
public:
    // 重写基类的 makeSound 方法
    void makeSound() override {
        cout << "汪汪汪！" << endl;
    }

    // 析构函数
    ~Dog() {
        cout << "Dog destructor called" << endl;
    }
};

// 派生类 Cat
class Cat : public Animal {
public:
    // 重写基类的 makeSound 方法
    void makeSound() override {
        cout << "喵喵喵！" << endl;
    }

    // 析构函数
    ~Cat() {
        cout << "Cat destructor called" << endl;
    }
};

int main() {
    // 使用基类指针指向派生类对象
    Animal* animal1 = new Dog();
    Animal* animal2 = new Cat();

    // 调用 makeSound 方法，多态行为：调用的是派生类的 makeSound
    cout << "animal1 发出声音：" << endl;
    animal1->makeSound();

    cout << "animal2 发出声音：" << endl;
    animal2->makeSound();

    // 删除对象，调用析构函数
    delete animal1;
    delete animal2;

    return 0;
}
```
#### 访问权限


```c++
class Base {
private:
    virtual void fun20() { cout << "Base::fun2()" << endl; }
};

class Derived : public Base {
public:
    virtual void fun20() { cout << "Derived::fun2()" << endl; }
};

int main(){
	Derived d;
	Base* pBase = &d;
	pBase->fun20(); 		// 编译器出错
}

```

C++访问权限的检查在编译时检查，此时会基于指针的静态类型，即`Base*`，而不是对象的动态类型`Derived`。

##### 访问权限的最佳实践

虚函数通常应在基类中声明为public或protected，以便外部或派生类可以访问。

如果基类中的虚函数是private，通常用于实现“模板方法模式”，但需要通过public的非虚函数间接调用。

#### 动态联编的性能

动态联编通过虚函数表实现，相比静态联编有少量性能开销（`vtable`查找），但提供了多态的灵活性。

### 8.3 多态与动态联编

#### 静态联编和动态联编

联编是将计算机程序中各个模块（函数）合并在一起生成可执行代码的过程。联编时对每个模块或者函数调用分配内存地址，并且对外部访问也分配正确的内存地址。按照进行联编的阶段不同可分为两种联编方法：**静态联编**和**动态联编**。

*   **静态联编 Static Binding**：在编译阶段就将函数实现和函数调用关联起来，也叫**早绑定Early Binding**。对函数的选择是基于指向对象的指针（或者引用）的类型
*   **动态联编 Dynamic Binding**：在程序执行时才将函数实现和函数调用相关联，因此也叫**运行时绑定或晚绑定Late Binding**。动态联编对函数的选择是基于对象类型，不是基于指针或者引用。C++中一般情况下是静态联编，涉及到多态和虚拟函数就是动态联编。

#### 静态类型和动态类型

*   静态类型：静态类型指对象声明时的类型或表达式生成时的类型，在编译时就已经确定。

    ```c++
    class Base { }
    Base *p; 						//指针p的静态类型为Base
    ```

*   动态类型：动态类型指 指针或引用所绑定的对象的类型，仅在运行时可知，例如：

    ```c++
    class Derived : public Base { };
    
    Derived d;
    
    Base *p = &d; 					//指针p的动态类型为Derived
    ```

### 8.4 虚函数的实现原理

   虚函数多态的实现原理主要依赖于虚函数表（vtable）和虚指针（vptr），以下是简要说明：

1.  虚函数表（vtable）：
    -   每个包含虚函数的类都会生成一个虚函数表，存储该类中所有虚函数的地址。
    -   虚函数表是一个静态数组，在编译时确定，运行时只读。
2.  虚指针（vptr）：
    -   每个对象实例在构造时会自动添加一个隐藏的虚指针，指向其所属类的虚函数表。
    -   虚指针通常存储在对象内存布局的开头。
3.  多态的调用过程：
    -   当通过基类指针或引用调用虚函数时，程序通过对象的虚指针找到对应的虚函数表。
    -   根据虚函数表中的地址，调用实际子类的虚函数实现。
4.  动态绑定：
    -   编译器无法在编译时确定具体调用哪个函数，因此通过虚函数表在运行时动态解析，称为动态绑定。
    -   非虚函数则通过静态绑定直接调用。
5.  关键点：
    -   构造函数和析构函数通常不建议声明为虚函数，因为构造时对象类型尚未完全确定，析构时可能导致未定义行为。
    -   虚函数调用会带来少量性能开销（间接寻址），但支持了面向对象的多态性。

### 8.5 多态中的构造函数与析构函数

#### 在构造函数和析构函数中调用虚函数

*   **在构造函数和析构函数中调用虚函数，不是多态。**它们编译时即确定调用的是自己的类或基类中定义的函数，不会等到运行时才决定。

*   在普通成员函数中调用虚函数，则是动态联编，是多态。

```c++
// 在构造函数和析构函数中不是多态
class myclas{
public:
    virtual void hello(){
    	cout<<"hello from myclass"<<endl;
    }
    virtual void bye(){
    	cout<<" bye from myclass"<<endl;
    }
};

class son : public myclass{
public:
    void hello(){
    	cout<<"hello from son"<<endl;
    }
    
    son(){ hello(); }
    ~son(){ bye(); }
};

class grandson : public son{
public:
    void hello(){
    	cout<<"hello from grandson"<<endl;
    }
    
    grandson(){ cout<<" constructing grandson"<<endl; }
    ~grandson(){ cout<<" deconstructing grandson"<<endl; }
};

int main(){
    grandson gson;
    son *pson = &gson;
    pson->hello();		// void grandson::hello()
    
	return 0;
}

// output:
//------------------
hello from son				//gson先创建son，son()中静态连接了son:helloO
constructing grandson		 //gson的创建
hello fromgrandson			 //pson->hello() 动态连接到grandson:helloO
destructing grandson		 //gson的创建的析构
bye from myclass			//gson中son部分的析构，～sonO中静态
						   //连接了myclass::byeO
```

#### 析构虚函数

在 C++ 中，虚析构函数（virtual destructor）是实现多态安全销毁对象的关键机制。以下是对其作用、用法和实践的详细说明：

##### 作用

虚析构函数的主要作用是**确保通过基类指针或引用删除派生类对象时，能够正确调用派生类的析构函数，避免内存泄漏或未定义行为。**

-   多态场景下的问题：
    -   **如果基类的析构函数不是虚函数，通过基类指针删除派生类对象时，只会调用基类的析构函数，派生类的析构函数不会被调用。**
    -   这可能导致派生类中动态分配的资源（如内存、文件句柄等）无法释放，引发内存泄漏或资源未清理。
-   虚析构函数的解决：
    -   **将基类的析构函数声明为 virtual，通过虚函数表（vtable）机制，确保运行时动态调用正确的派生类析构函数。**
    -   **派生类的析构函数会自动成为虚函数（无需显式声明 virtual）**
-   其他作用：
    -   支持多态继承体系的安全性。
    -   保证对象生命周期管理的正确性。

##### 用法

```cpp
class Base {
public:
    virtual ~Base() {
        // 基类资源清理
        std::cout << "Base destructor" << std::endl;
    }
};

class Derived : public Base {
public:
    ~Derived() override {
        // 派生类资源清理
        std::cout << "Derived destructor" << std::endl;
    }
};
```

-   通过基类指针删除对象：

    ```cpp
    Base* ptr = new Derived();
    delete ptr; // 正确调用 Derived::~Derived()，然后调用 Base::~Base()
    ```

-   多态容器： 使用 std::vector<Base*> 或其他容器存储多态对象时，确保析构时调用正确的析构函数。

-   抽象基类： 抽象类（包含纯虚函数）通常需要虚析构函数以支持派生类的正确销毁。

##### 析构顺序

-   派生类的析构函数先执行，清理派生类资源。
-   随后自动调用基类的析构函数，清理基类资源。
-   析构顺序与构造顺序相反。

##### 实践

###### 何时需要虚析构函数

-   需要：
    -   基类设计为支持多态（通过基类指针或引用操作派生类对象）。
    -   派生类可能分配动态资源（如内存、文件、socket等）。
    -   类层次结构中可能被继承，且存在动态对象管理。
-   不需要：
    -   类不用于多态（如 final 类或非继承场景）。
    -   所有资源管理通过智能指针（如 std::shared_ptr）等自动处理，且无手动 delete。
    -   基类和派生类都没有需要清理的资源。

###### 性能考量

-   开销：
    -   虚析构函数引入虚函数表（vtable）和虚指针（vptr），增加少量内存和性能开销。
    -   对于小型、性能敏感的类（如嵌入式系统），可评估是否需要多态。
-   实践建议：
    -   如果类可能被继承，建议默认将析构函数设为虚函数，除非明确不需要多态。
    -   对于性能敏感场景，可使用 final 关键字禁止继承，移除虚函数表开销。

###### 纯虚析构函数

-   如果基类是抽象类，可以将析构函数声明为纯虚函数，但必须提供实现：

    ```cpp
    class AbstractBase {
    public:
        virtual ~AbstractBase() = 0; // 纯虚析构函数
    };
    
    AbstractBase::~AbstractBase() {
        // 实现不能为空
        std::cout << "AbstractBase destructor" << std::endl;
    }
    ```
    
-   用途：强制派生类提供析构逻辑，同时保持基类的抽象性。

###### 智能指针与虚析构函数

-   使用 std::shared_ptr 或 std::unique_ptr 管理多态对象时，虚析构函数仍然必要：

    ```cpp
    std::unique_ptr<Base> ptr = std::make_unique<Derived>();
    // 析构时需要虚析构函数确保调用 Derived::~Derived()
    ```
    
-   智能指针简化了手动 delete，但不改变多态析构的规则。

###### 常见错误

-   非虚析构函数：

    ```cpp
    class Base {
    public:
        ~Base() { std::cout << "Base destructor" << std::endl; }
    };
    
    class Derived : public Base {
    public:
        ~Derived() { std::cout << "Derived destructor" << std::endl; }
    };
    
    Base* ptr = new Derived();
    delete ptr; // 只调用 Base::~Base()，Derived::~Derived() 被跳过
    ```
    
    -   后果：内存泄漏或资源未释放。
    
-   遗忘基类析构函数：

    -   如果基类析构函数未定义，编译器生成默认析构函数（非虚），可能导致多态析构失败。

###### 实践示例

以下是一个完整的多态示例，展示虚析构函数的作用：

```cpp
#include <iostream>
#include <vector>

class Animal {
public:
    virtual ~Animal() { std::cout << "Animal destructor" << std::endl; }
    virtual void speak() const = 0; // 纯虚函数
};

class Dog : public Animal {
public:
    ~Dog() override { std::cout << "Dog destructor" << std::endl; }
    void speak() const override { std::cout << "Woof!" << std::endl; }
};

class Cat : public Animal {
public:
    ~Cat() override { std::cout << "Cat destructor" << std::endl; }
    void speak() const override { std::cout << "Meow!" << std::endl; }
};

int main() {
    std::vector<Animal*> animals;
    animals.push_back(new Dog());
    animals.push_back(new Cat());

    for (const auto* animal : animals) {
        animal->speak();
    }

    for (auto* animal : animals) {
        delete animal; // 虚析构函数确保正确调用 Dog::~Dog() 和 Cat::~Cat()
    }

    return 0;
}
```

输出：

```text
Woof!
Meow!
Cat destructor
Animal destructor
Dog destructor
Animal destructor
```

------

4.   实践建议

-   设计基类时：
    -   默认将析构函数设为 virtual，除非明确不需要多态。
    -   如果类不打算被继承，标记为 final 或保持非虚析构。
-   代码审查：
    -   检查基类是否有多态用途，若有，确保析构函数为虚函数。
    -   验证派生类是否正确管理资源。
-   工具使用：
    -   使用静态分析工具（如 Clang-Tidy）检测非虚析构函数的潜在问题。
    -   结合智能指针减少手动内存管理错误。
-   性能优化：
    -   对于不需要多态的小型类，避免虚函数表开销。
    -   在嵌入式或高性能场景中，权衡多态与资源消耗。

------

总结

-   作用：虚析构函数确保多态场景下派生类对象正确销毁，避免资源泄漏。
-   用法：在基类中声明 virtual ~Base()，派生类自动继承虚析构行为。
-   实践：适用于多态继承体系，需注意性能开销和正确性，结合智能指针简化管理。

如果有具体场景或代码需要进一步分析，请提供更多细节！

### 8.6 纯虚函数与抽象类

*   纯虚函数：没有函数体的虚函数。
*   抽象类：含有纯虚函数的类。

因为纯虚函数一定要被派生类重写，所以抽象类不能用来创建抽象类对象。

抽象类的成员函数可以调用纯虚函数，但是在构造函数和析构函数内部不可以调用纯虚函数。

如果一个类继承了一个抽象类，那么它只有实现了所有的纯虚函数，才能称为非抽象类，才能实例化对象。

>   [!NOTE]
>
>   实践中：
>
>   1.   一般不要重新定义基类非虚函数
>
>   2.   如果需要重新定义基类函数，则该函数应声明为虚函数
>
>   3.   派生类继承基类非虚函数的接口和实现、虚函数的接口和默认实现，以及纯虚函数的接口。

















