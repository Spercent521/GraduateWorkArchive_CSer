*   继承和复合的区别
*   继承和派生中的访问控制
*   派生类的构造和析构函数
*   多重继承和多级继承
*   派生类和基类的复制兼容规则
*   虚基类和虚继承（选读）

### 7.1 继承与派生的概念

在已有类（**基类base class / 超类super class**）特性的基础上进行扩展，增加属性和功能，定义出新的类（派生类derived class/子类subclass）。

代码重用的机制：预定义，预编译，标准函数，类库，类模板，类继承。其中最重要的是 **继承**

#### 派生类与基类的关系

*   派生类定义后，独立使用，不依赖于基类

*   **派生类拥有基类的全部成员**（包括private、protected、public属性和private、protected、public方法）

*   派生类的各个成员函数不能访问基类中的private成员。

#### 访问权限

`public,protected,private`

| 访问权限  | 类内部 | 派生类   | 外部     |
| --------- | ------ | -------- | -------- |
| public    | 可访问 | 可访问   | 可访问   |
| protected | 可访问 | 可访问   | 不可访问 |
| private   | 可访问 | 不可访问 | 不可访问 |

##### public

通常用于定义类的外部接口，比如提供给用户调用的函数或需要外部直接访问的数据。

```c++
class MyClass {
public:
    int publicVar; // 公有成员变量
    void publicFunc() { // 公有成员函数
        std::cout << "This is public" << std::endl;
    }
};

int main() {
    MyClass obj;
    obj.publicVar = 10; // 可以直接访问
    obj.publicFunc();   // 可以直接调用
    return 0;
}
```

##### protected

通常用于需要在基类和派生类之间共享的数据或函数，但不希望外部直接访问。

```c++
class Base {
protected:
    int protectedVar; // 受保护成员变量
public:
    Base() { protectedVar = 20; }
};

class Derived : public Base {
public:
    void print() {
        std::cout << protectedVar << std::endl; // 派生类可以访问
    }
};

int main() {
    Base b;
    // b.protectedVar = 30; // 错误！外部无法访问
    Derived d;
    d.print(); // 输出 20
    return 0;
}
```

##### private

访问权限全都变成private

```c++
class MyClass {
private:
    int privateVar; // 私有成员变量
public:
    MyClass() { privateVar = 30; }
    int getVar() { return privateVar; } // 通过公有函数访问私有成员
};

int main() {
    MyClass obj;
    // obj.privateVar = 40; // 错误！外部无法访问
    std::cout << obj.getVar() << std::endl; // 输出 30
    return 0;
}
```

#### final

可以利用C++11提供的关键字final来**阻止继承**的发生：

```c++
class NoDerived final{}; 		// NoDerived不能作为基类被继承
```

#### 命名冲突与覆盖

如果派生类成员的名字和基类的成员名字相同，那么定义在派生类（内层作用域）的名字将会覆盖（屏蔽）掉基类（外层作用域）的名字：

```c++
class Base {
protected:
	int m_data;
public:
	void foo(int) { /*...*/ }
};
class Derived : public Base {
protected:
	int m_data; 					//基类m_data被覆盖
public:
	void foo(int) { /*...*/ }		 //基类foo成员被覆盖
int foo() { 					    //函数重载
		return m_data; 			    //返回Derived::m_data
	}
};
```

如果在派生类里面需要访问基类的同名成员，则可以使用基类的作用域运算符：

```c++
class Derived : public Base {
	int foo() { return Base::m_data; } //返回Base中的m_data
};
```

**避免在派生类和基类中使用同名成员变量。同名成员函数比较常见。**

#### 派生类对象和基类对象

1.   派生类对象占用的存储空间 = 基类对象占用的存储空间 + 派生类自身扩充的成员变量占用的存储空间

2.   派生类对象中包含着基类对象（匿名，逻辑结构上包含），并且基类对象存储位置位于派生类对象新增成员之前

![image-20250409151956487](D:\universityResource\_课程\C++编程_大一下\pic\派生和继承的存储结构.png)

#### 继承与复合的区别

继承是指子类继承了父类的内容，符合是指把一个类当作另一个类的一部分。

```c++
class Student : public Person { //学生类，公有继承Person

private:
    
    /* 复合 */
	Course m_course;

public:
	Student(const string &name, int age, const Course &c):
	Person(name, age), m_course(c) {}
	Course& course() { return m_course; }
};

```

#### const关键字

形参前的`const`：确保函数内部不会修改传入的参数。

函数头部前的`const`：确保函数返回的值不会被修改。

函数头和函数体之间的`const`：确保该成员函数不会修改类对象的成员变量。

### 7.2 继承与派生中的访问控制

#### 继承方式

在继承的时候，如果不显式声明，会按照**private**继承。

##### public继承

*   基类的public成员在派生类中保持public，外部和派生类均可访问。

*   基类的protected成员在派生类中保持protected，派生类内部可访问，外部不可访问。

*   基类的private成员在派生类中不可直接访问。

```c++
// public 继承
class Base {
public:
    int publicVar;
protected:
    int protectedVar;
private:
    int privateVar;
};

class DerivedPublic : public Base {
public:
    void test() {
        publicVar = 1;     // 可访问
        protectedVar = 2;  // 可访问
        // privateVar = 3; // 错误
    }
};

int main() {
    DerivedPublic dp;
    dp.publicVar = 10;     // 可以，public继承

    return 0;
}
```

##### protected继承

*   基类的public成员在派生类中变为protected，派生类内部可访问，外部不可访问。

*   基类的protected成员在派生类中保持protected，派生类内部可访问。

*   基类的private成员在派生类中不可直接访问。

```c++
/* protected */

class Base {
public:
    int publicVar;
protected:
    int protectedVar;
};

class DerivedProtected : protected Base {
public:
    void test() {
        publicVar = 1;     // 可访问
        protectedVar = 2;  // 可访问
    }
};

int main() {
    DerivedProtected dp;
    
    /* protected 继承以后最高权限是 protected */
    /* 相当于把所有的 public 也变成 protected */
    
    // dp.publicVar = 10;  // 错误，protected继承

    return 0;
}
```

##### private继承

这是默认的继承方式，将所有的权限变成**private**。

#### using

通过使用**using**声明，可以**改变**派生类中基类成员的访问权限：

```c++
class Base {
private:
	int m_pri; //private成员
protected:
	int m_pro; //protected成员
public:
	int m_pub; //public成员
};


class PubDerv : public Base {
public:
    
    /* 声明为公有的 */
    
    using base::m_pro;
};

void test() {
	PubDerv d;
	d.m_pro; 				//正确
}

```

### 7.3 复杂继承方法

#### 多重继承

$\textit{Def}$：如果一个派生类同时有多个直接基类，称为**多重继承multiple-inheritance**，这时的派生类同时继承了多个已有基类的特征。

```c++
/* 由圆和高多重继承派生出圆锥 */

class Circle{                            //定义园类
protected:
    float x,y,r; //(x,y)为圆心,r为半径
public:
   Circle(float a=0,float b=0,float R=0){x=a;y=b;r=R;}
   void Setcoordinate(float a,float b){x=a;y=b;}    //设置圆心坐标
   void Getcoordinate(float &a,float &b){a=x;b=y;} 
   void SetR(float R){r=R;}	//设置半径
   float GetR(){return r;}	//取圆半径
   float GetAreaCircle(){return float(r*r*3.14159);}     //取圆面积
   float GetCircumference(){return float(2*r*3.14159);}    //取圆周长
};

class Line{          //定义高类
protected:
   float High;
public:
   Line(float a=0){High=a;}
   void SetHigh(float a){High=a;}
   float GetHigh(){return High;}
};

class Cone : public Circle , public Line{         //派生类圆锥同时继承高和园
public:
   Cone(float a,float b,float R,float d):Circle(a,b,R),Line(d){  }
   float GetCV(){return float(GetAreaCircle()*High/3);}
     //取得圆锥体积
   float GetCA(){ //取得圆锥表面积
   	return  float(GetAreaCircle()+r*3.14159*sqrt(r*r+High*Hgih));
}; //共有派生类中能直接访问直接基类的保护成员

```

#### 多层继承

$\textit{Def}$：派生出来的新类同样可以作为基类再继续派生出更新的类，依此类推形成一个层次结构，称为多层继承。直接参与派生出某类称为直接基类，而基类的基类，以及更深层的基类称为间接基类。也就是说，**派生类的上一层是直接基类，再上面就是间接基类了**。

### 7.4 派生类的构造函数与析构函数

**派生类应定义自己的构造函数和析构函数**，避免编译出错。

在C++中，派生类的构造函数和析构函数的构造需要特别注意，因为派生类不仅需要初始化自身的成员，还需要通过基类的构造函数初始化基类的成员。同时，析构函数的调用顺序也需要理解，以确保资源正确释放。

#### 派生类对象构造函数的执行顺序

1.   先执行基类的构造函数
2.   再执行成员对象类的构造函数
3.   最后执行派生类自己的构造函数

*   多重继承会按顺序执行以上。

*   在多层继承中，构造函数的调用顺序是从最顶层基类开始，逐层向下，直到派生类自身。

*   如果派生类有**类**类型的**成员对象**，这些成员对象的构造函数会在派生类构造函数体执行前调用（按照声明顺序）。

#### 派生类对象析构函数的执行顺序

与构造相反：析构函数的调用顺序与构造函数相反，先析构派生类自身，再析构基类。

多层继承：从最底层的派生类开始，逐层向上调用基类的析构函数。

成员对象：**成员对象的析构函数在派生类析构函数体执行后调用**（按照声明顺序的逆序）

也就是说：

1.   派生类自己的析构
2.   派生类中成员对象的析构
3.   基类的析构

### 二义性问题

在多重继承的时候，如果基类里面有相同名称的变量，那么在调用时很难区分。可以用`::`来限定或者使用虚继承。



# 还有复制等问题





